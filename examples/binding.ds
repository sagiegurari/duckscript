
out = set "Hello World"

list = array ${out}
for entry in ${list}
    echo current entry is: ${entry}
end
release list

list = array %{out}
for entry in ${list}
    echo current entry is: ${entry}
end
release list
