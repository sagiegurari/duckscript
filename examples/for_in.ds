
args = array a b c
echo array handle: ${args}

for arg in args
    echo current arg is: ${arg}
end_for
release args

values = range 1 10

for i in values
    for j in values
        echo i: ${i} j: ${j}
    end_for
end_for

release values