
[env]
CARGO_MAKE_EXTEND_WORKSPACE_MAKEFILE = "true"

[config]
additional_profiles = [
    "ci-coverage-tasks",
    "ci-all-build-tasks",
    "publish-pre-cleanup"
]

[tasks.generate-sdk-docs]
workspace = false
command = "cargo"
args = [ "run", "--", "./sdkdocs.ds", ]
