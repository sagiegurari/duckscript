
fn test_uname
    name = os_name
    info = uname
    assert_eq ${name} ${info}
end

fn test_uname_extended
    name = os_name
    release = os_release
    version = os_version
    info = uname -a
    assert_eq "${name} ${release} ${version}" ${info}
end
