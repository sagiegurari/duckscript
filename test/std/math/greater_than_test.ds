
fn test_equals
    result = greater_than 1 1

    assert_false ${result}
end

fn test_less_than
    result = greater_than 1 1.5

    assert_false ${result}
end

fn test_greater_than
    result = greater_than 2 1.5

    assert ${result}
end
